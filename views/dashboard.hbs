<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <script src="https://kit.fontawesome.com/6bf61ced32.js" crossorigin="anonymous"></script>
    {{!-- <link rel="stylesheet" href="main.css"> --}}
</head>
<body>
    <header>
        <h3>Tarik-Pe-Tarik...</h3>
        <nav class="login">
            <ul class="nav-links">
                <li><a href="/add">Add</a></li>   
                <li><a href="/logout">Logout</a></li>   
            </ul>
        <nav>
    </header>
    
    <h6>Dashboard</h6>
    <p>All events</p>
    {{#if events}}
    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Criteria</th>
                <th>Mode</th>
                <th>Reg. Date</th>
                <th>Test Date</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            {{#each events}}
            <tr>
                <td>{{name}}</td>
                <td>{{eligibility}}</td>
                <td>{{mode}}</td>
                <td>{{formatDate registrationDate 'Do MMMM YYYY'}}</td>
                <td>{{formatDate testDate 'Do MMMM YYYY'}}</td>
                <td>
                    <a href="/edit/{{_id}}" class="btn btn-float">
                        <i class="fas fa-edit"></i>
                    </a>

                    <form action="/events/{{_id}}" method="POST" id="delete-form">
                        <input type="hidden" name="_method" value="DELETE">
                        <button type="submit" class="btn red">
                            <i class="fas fa-trash"></i>
                        </button>
                    </form>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
    {{else}}
    <p>You have not created any event</p>
    {{/if}}
    <footer>
        <p>A project by Vaibhav Gupta</p>
    </footer>
</body>
</html>